---
name: git-hex-status
description: Check git-hex state (conflicts, backups, in-progress operations)
---

I'll check the current git-hex state for you.

**Checking:**
1. Any in-progress rebase/merge/cherry-pick operations
2. Conflict status if an operation is paused
3. Available backup refs for undo

Let me run `git-hex-getConflictStatus` to see the current state...

